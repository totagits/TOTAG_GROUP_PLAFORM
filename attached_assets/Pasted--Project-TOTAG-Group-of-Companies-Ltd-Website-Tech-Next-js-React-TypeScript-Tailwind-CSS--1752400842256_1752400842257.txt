/*
Project: TOTAG Group of Companies Ltd Website
Tech: Next.js (React), TypeScript, Tailwind CSS, shadcn/ui, Framer Motion
Structure:
  /components
    - Header.tsx (with mega-menu)
    - Footer.tsx
    - ServiceCard.tsx
    - ContactForm.tsx
  /pages
    - _app.tsx
    - index.tsx
    - cargo.tsx
    - farm.tsx
    - petroleum.tsx
    - construction.tsx
    - merchandise.tsx
    - it.tsx
    - catering.tsx
    - api/contact.ts
  tailwind.config.js
  postcss.config.js
  next.config.js
*/

// package.json (snippet)
{
  "name": "totag-group-website",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "shadcn-ui": "latest",
    "framer-motion": "^8.0.0",
    "nodemailer": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "tailwindcss": "^3.0.0",
    "autoprefixer": "^10.0.0",
    "postcss": "^8.0.0",
    "eslint": "^8.0.0",
    "eslint-config-next": "^13.0.0"
  }
}

// tailwind.config.js
module.exports = {
  content: ["./pages/**/*.{js,ts,jsx,tsx}", "./components/**/*.{js,ts,jsx,tsx}"],
  theme: { extend: {} },
  plugins: [],
};

// postcss.config.js
module.exports = { plugins: { tailwindcss: {}, autoprefixer: {} } };

// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = { reactStrictMode: true };
module.exports = nextConfig;

// components/Header.tsx
import Link from 'next/link';
import { useState } from 'react';
import { motion, AnimatePresence } from 'framer-motion';

const services = [
  { name: 'Cargo Handling', href: '/cargo' },
  { name: 'FARM', href: '/farm' },
  { name: 'Petroleum Services', href: '/petroleum' },
  { name: 'General Construction', href: '/construction' },
  { name: 'General Merchandise', href: '/merchandise' },
  { name: 'IT Services', href: '/it' },
  { name: 'Catering Services', href: '/catering' },
];

export default function Header() {
  const [open, setOpen] = useState(false);
  return (
    <header className="bg-white shadow-md fixed w-full z-50">
      <div className="container mx-auto flex items-center justify-between p-4">
        <Link href="/"><a className="text-2xl font-bold">TOTAG Group</a></Link>
        <nav className="flex items-center space-x-6">
          <div className="relative">
            <button onMouseEnter={() => setOpen(true)} onMouseLeave={() => setOpen(false)} className="px-4 py-2 font-medium">
              Services
            </button>
            <AnimatePresence>
              {open && (
                <motion.div
                  onMouseEnter={() => setOpen(true)}
                  onMouseLeave={() => setOpen(false)}
                  initial={{ opacity: 0, y: -10 }}
                  animate={{ opacity: 1, y: 0 }}
                  exit={{ opacity: 0, y: -10 }}
                  className="absolute left-0 mt-2 w-screen max-w-md bg-white shadow-lg rounded-xl z-50"
                >
                  <div className="grid grid-cols-2 gap-4 p-6">
                    {services.map(s => (
                      <Link key={s.href} href={s.href}>
                        <a className="block p-2 hover:bg-gray-100 rounded">{s.name}</a>
                      </Link>
                    ))}
                  </div>
                </motion.div>
              )}
            </AnimatePresence>
          </div>
          <Link href="/#contact"><a className="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition">Contact</a></Link>
        </nav>
      </div>
    </header>
  );
}

// components/ServiceCard.tsx (unchanged)
import { motion } from 'framer-motion';
export interface ServiceCardProps { title: string; description: string; id: string; }
export default function ServiceCard({ title, description, id }: ServiceCardProps) {
  return (
    <motion.div id={id} className="p-6 bg-white rounded-2xl shadow-md" whileHover={{ scale: 1.03 }} transition={{ duration: 0.3 }}>
      <h3 className="text-xl font-semibold mb-2">{title}</h3>
      <p className="text-gray-600">{description}</p>
    </motion.div>
  );
}

// components/ContactForm.tsx
import { useState } from 'react';
export default function ContactForm() {
  const [status, setStatus] = useState<'idle'|'sending'|'success'|'error'>('idle');
  const [form, setForm] = useState({ name: '', email: '', message: '' });
  const handleChange = (e: React.ChangeEvent<HTMLInputElement|HTMLTextAreaElement>) => {
    setForm({ ...form, [e.target.name]: e.target.value });
  };
  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault(); setStatus('sending');
    try {
      const res = await fetch('/api/contact', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify(form) });
      if (res.ok) setStatus('success'); else throw new Error();
    } catch {
      setStatus('error');
    }
  };
  return (
    <form onSubmit={handleSubmit} className="max-w-xl mx-auto p-6 bg-white rounded-lg shadow">
      <h2 className="text-2xl font-semibold mb-4 text-center">Contact Us</h2>
      {status==='success' && <p className="mb-4 text-green-600">Thanks for reaching out!</p>}
      {status==='error' && <p className="mb-4 text-red-600">Something went wrong.</p>}
      <input name="name" placeholder="Name" required value={form.name} onChange={handleChange} className="w-full mb-3 p-2 border rounded" />
      <input type="email" name="email" placeholder="Email" required value={form.email} onChange={handleChange} className="w-full mb-3 p-2 border rounded" />
      <textarea name="message" placeholder="Message" required value={form.message} onChange={handleChange} className="w-full mb-3 p-2 border rounded h-32"></textarea>
      <button type="submit" disabled={status==='sending'} className="w-full py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition">
        {status==='sending' ? 'Sending...' : 'Send Message'}
      </button>
    </form>
  );
}

// components/Footer.tsx (unchanged)
export default function Footer() {
  return (
    <footer className="bg-gray-800 text-white py-6 mt-16">
      <div className="container mx-auto text-center">
        <p>&copy; {new Date().getFullYear()} TOTAG Group of Companies Ltd. All rights reserved.</p>
      </div>
    </footer>
  );
}

// pages/api/contact.ts
import type { NextApiRequest, NextApiResponse } from 'next';
import nodemailer from 'nodemailer';
export default async function handler(req: NextApiRequest, res: NextApiResponse) {
  if (req.method !== 'POST') return res.status(405).end();
  const { name, email, message } = req.body;
  if (!name||!email||!message) return res.status(400).json({ error: 'All fields are required' });
  // configure SMTP transporter (replace with real credentials)
  const transporter = nodemailer.createTransport({ host: process.env.SMTP_HOST, port: 587, auth: { user: process.env.SMTP_USER, pass: process.env.SMTP_PASS } });
  try {
    await transporter.sendMail({ from: `"TOTAG Website" <${process.env.SMTP_USER}>`, to: 'info@totag-group.com', subject: `Contact from ${name}`, text: message, html: `<p>${message}</p><p>— ${name} (${email})</p>` });
    return res.status(200).json({ success: true });
  } catch (err) {
    return res.status(500).json({ error: 'Mail send failed' });
  }
}

// pages/index.tsx (unchanged)
import Header from '../components/Header';
import ServiceCard from '../components/ServiceCard';
import Footer from '../components/Footer';

const services = [
  { id: 'cargo', title: 'TOTAG Cargo Handling', description: 'Efficient and secure cargo logistics and port management.' },
  { id: 'farm', title: 'TOTAG FARM', description: 'Integrated agribusiness solutions from seed to market.' },
  { id: 'petroleum', title: 'TOTAG Petroleum Services', description: 'Professional fuel distribution and petroleum logistics.' },
  { id: 'construction', title: 'TOTAG General Construction', description: 'Quality civil works, infrastructure, and project management.' },
  { id: 'merchandise', title: 'TOTAG General Merchandise', description: 'Wholesale and retail supply of diversified goods.' },
  { id: 'it', title: 'TOTAG IT Services', description: 'Custom IT solutions, support, and digital transformation.' },
  { id: 'catering', title: 'TOTAG Catering Services', description: 'Professional catering and event hospitality services.' },
];

export default function Home() {
  return (
    <>
      <Header />
      <main className="pt-24 container mx-auto px-4">
        <section className="text-center py-16">
          <h1 className="text-4xl font-bold mb-4">Welcome to TOTAG Group</h1>
          <p className="text-gray-700 max-w-2xl mx-auto">Excellence across industries through our seven specialized subsidiaries.</p>
        </section>
        <section className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {services.map(s => <ServiceCard key={s.id} {...s} />)}
        </section>
      </main>
      <section id="contact" className="py-16 bg-gray-50">
        <ContactForm />
      </section>
      <Footer />
    </>
  );
}

// pages/cargo.tsx
import Header from '../components/Header'; import Footer from '../components/Footer'; import ContactForm from '../components/ContactForm';
export default function Cargo() {
  return (
    <>
      <Header />
      <main className="pt-24 container mx-auto px-4">
        <h1 className="text-3xl font-bold mb-4">TOTAG Cargo Handling</h1>
        <p className="text-gray-700 mb-6">We provide end-to-end cargo logistics solutions...</p>
      </main>
      <section className="py-16 bg-gray-50"><ContactForm /></section>
      <Footer />
    </>
  );
}

// pages/farm.tsx (similarly)
import Header from '../components/Header'; import Footer from '../components/Footer'; import ContactForm from '../components/ContactForm';
export default function Farm() {
  return (<><Header /><main className="pt-24 container mx-auto px-4"><h1 className="text-3xl font-bold mb-4">TOTAG FARM</h1><p className="text-gray-700 mb-6">Integrated agribusiness from seed to market...</p></main><section className="py-16 bg-gray-50"><ContactForm/></section><Footer/></>);
}

// pages/petroleum.tsx
import Header from '../components/Header'; import Footer from '../components/Footer'; import ContactForm from '../components/ContactForm';
export default function Petroleum() {
  return (<><Header /><main className="pt-24 container mx-auto px-4"><h1 className="text-3xl font-bold mb-4">TOTAG Petroleum Services</h1><p className="text-gray-700 mb-6">Reliable fuel distribution & logistics...</p></main><section className="py-16 bg-gray-50"><ContactForm/></section><Footer/></>);
}

// pages/construction.tsx
import Header from '../components/Header'; import Footer from '../components/Footer'; import ContactForm from '../components/ContactForm';
export default function Construction() {
  return (<><Header /><main className="pt-24 container mx-auto px-4"><h1 className="text-3xl font-bold mb-4">TOTAG General Construction</h1><p className="text-gray-700 mb-6">Quality civil works & infrastructure projects...</p></main><section className="py-16 bg-gray-50"><ContactForm/></section><Footer/></>);
}

// pages/merchandise.tsx
import Header from '../components/Header'; import Footer from '../components/Footer'; import ContactForm from '../components/ContactForm';
export default function Merchandise() {
  return (<><Header /><main className="pt-24 container mx-auto px-4"><h1 className="text-3xl font-bold mb-4">TOTAG General Merchandise</h1><p className="text-gray-700 mb-6">Wholesale & retail supply solutions...</p></main><section className="py-16 bg-gray-50"><ContactForm/></section><Footer/></>);
}

// pages/it.tsx
import Header from '../components/Header'; import Footer from '../components/Footer'; import ContactForm from '../components/ContactForm';
export default function IT() {
  return (<><Header /><main className="pt-24 container mx-auto px-4"><h1 className="text-3xl font-bold mb-4">TOTAG IT Services</h1><p className="text-gray-700 mb-6">Custom IT & digital transformation...</p></main><section className="py-16 bg-gray-50"><ContactForm/></section><Footer/></>);
}

// pages/catering.tsx
import Header from '../components/Header'; import Footer from '../components/Footer'; import ContactForm from '../components/ContactForm';
export default function Catering() {
  return (<><Header /><main className="pt-24 container mx-auto px-4"><h1 className="text-3xl font-bold mb-4">TOTAG Catering Services</h1><p className="text-gray-700 mb-6">Event catering & hospitality excellence...</p></main><section className="py-16 bg-gray-50"><ContactForm/></section><Footer/></>);
}

/* styles/globals.css */
@tailwind base;
@tailwind components;
@tailwind utilities;
