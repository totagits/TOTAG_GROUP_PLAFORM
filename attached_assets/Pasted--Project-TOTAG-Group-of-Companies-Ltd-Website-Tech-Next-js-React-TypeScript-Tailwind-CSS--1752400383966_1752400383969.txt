/*
Project: TOTAG Group of Companies Ltd Website
Tech: Next.js (React), TypeScript, Tailwind CSS, shadcn/ui, Framer Motion
Structure:
  /components
    - Header.tsx
    - Footer.tsx
    - ServiceCard.tsx
  /pages
    - _app.tsx
    - index.tsx
  tailwind.config.js
  postcss.config.js
  next.config.js
*/

// package.json (snippet)
{
  "name": "totag-group-website",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "shadcn-ui": "latest",
    "framer-motion": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "tailwindcss": "^3.0.0",
    "autoprefixer": "^10.0.0",
    "postcss": "^8.0.0",
    "eslint": "^8.0.0",
    "eslint-config-next": "^13.0.0"
  }
}

// tailwind.config.js
module.exports = {
  content: ["./pages/**/*.{js,ts,jsx,tsx}", "./components/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};

// postcss.config.js
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};
module.exports = nextConfig;

// components/Header.tsx
import Link from 'next/link';
import { motion } from 'framer-motion';

const navItems = [
  { name: 'Home', href: '/' },
  { name: 'Cargo Handling', href: '/#cargo' },
  { name: 'FARM', href: '/#farm' },
  { name: 'Petroleum', href: '/#petroleum' },
  { name: 'Construction', href: '/#construction' },
  { name: 'Merchandise', href: '/#merchandise' },
  { name: 'IT Services', href: '/#it' },
  { name: 'Catering', href: '/#catering' },
  { name: 'Contact', href: '/#contact' },
];

export default function Header() {
  return (
    <header className="bg-white shadow-md fixed w-full z-50">
      <div className="container mx-auto flex items-center justify-between p-4">
        <Link href="/">
          <a className="text-2xl font-bold">TOTAG Group</a>
        </Link>
        <nav>
          <ul className="flex space-x-6">
            {navItems.map(item => (
              <li key={item.href}>
                <Link href={item.href}>
                  <a className="hover:text-blue-600 transition">
                    {item.name}
                  </a>
                </Link>
              </li>
            ))}
          </ul>
        </nav>
      </div>
    </header>
  );
}

// components/ServiceCard.tsx
import { motion } from 'framer-motion';

export interface ServiceCardProps {
  title: string;
  description: string;
  id: string;
}

export default function ServiceCard({ title, description, id }: ServiceCardProps) {
  return (
    <motion.div
      id={id}
      className="p-6 bg-white rounded-2xl shadow-md"
      whileHover={{ scale: 1.03 }}
      transition={{ duration: 0.3 }}
    >
      <h3 className="text-xl font-semibold mb-2">{title}</h3>
      <p className="text-gray-600">{description}</p>
    </motion.div>
  );
}

// components/Footer.tsx
export default function Footer() {
  return (
    <footer className="bg-gray-800 text-white py-6 mt-16">
      <div className="container mx-auto text-center">
        <p>&copy; {new Date().getFullYear()} TOTAG Group of Companies Ltd. All rights reserved.</p>
      </div>
    </footer>
  );
}

// pages/_app.tsx
import '../styles/globals.css';
import type { AppProps } from 'next/app';

export default function App({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />;
}

// pages/index.tsx
import Header from '../components/Header';
import ServiceCard from '../components/ServiceCard';
import Footer from '../components/Footer';

const services = [
  { id: 'cargo', title: 'TOTAG Cargo Handling', description: 'Efficient and secure cargo logistics and port management.' },
  { id: 'farm', title: 'TOTAG FARM', description: 'Integrated agribusiness solutions from seed to market.' },
  { id: 'petroleum', title: 'TOTAG Petroleum Services', description: 'Professional fuel distribution and petroleum logistics.' },
  { id: 'construction', title: 'TOTAG General Construction', description: 'Quality civil works, infrastructure, and project management.' },
  { id: 'merchandise', title: 'TOTAG General Merchandise', description: 'Wholesale and retail supply of diversified goods.' },
  { id: 'it', title: 'TOTAG IT Services', description: 'Custom IT solutions, support, and digital transformation.' },
  { id: 'catering', title: 'TOTAG Catering Services', description: 'Professional catering and event hospitality services.' },
];

export default function Home() {
  return (
    <>
      <Header />
      <main className="pt-24 container mx-auto px-4">
        <section className="text-center py-16">
          <h1 className="text-4xl font-bold mb-4">Welcome to TOTAG Group</h1>
          <p className="text-gray-700 max-w-2xl mx-auto">Delivering excellence across diverse industries through our seven specialized subsidiaries.</p>
        </section>
        <section className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {services.map(s => (
            <ServiceCard key={s.id} {...s} />
          ))}
        </section>
        <section id="contact" className="mt-16 text-center">
          <h2 className="text-2xl font-semibold mb-4">Get in Touch</h2>
          <p className="mb-4">Email: info@totag-group.com | Phone: +231 77 555 0000</p>
          <a href="mailto:info@totag-group.com" className="inline-block px-6 py-3 bg-blue-600 text-white rounded-2xl hover:bg-blue-700 transition">
            Contact Us
          </a>
        </section>
      </main>
      <Footer />
    </>
  );
}

/* styles/globals.css */
@tailwind base;
@tailwind components;
@tailwind utilities;
