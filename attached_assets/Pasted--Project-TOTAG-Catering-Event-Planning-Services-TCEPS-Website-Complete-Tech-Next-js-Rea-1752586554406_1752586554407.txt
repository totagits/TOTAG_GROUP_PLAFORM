/*
Project: TOTAG Catering & Event Planning Services (TCEPS) Website - Complete
Tech: Next.js (React), TypeScript, Tailwind CSS, Framer Motion
Structure:
  /components
    - Header.tsx
    - Footer.tsx
    - ServiceCard.tsx
    - ContactForm.tsx
  /pages
    - _app.tsx
    - index.tsx
    - corporate-catering.tsx
    - event-planning.tsx
    - social-celebrations.tsx
    - specialty-menus.tsx
    - beverage-services.tsx
    - on-site-coordination.tsx
    - post-event-wrapup.tsx
    - api/contact.ts
  /public
    - logo.svg
  /styles
    - globals.css
  tailwind.config.js
  postcss.config.js
  next.config.js
  package.json
*/

// package.json
{
  "name": "totag-tceps-website",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "framer-motion": "^8.0.0",
    "nodemailer": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "tailwindcss": "^3.0.0",
    "autoprefixer": "^10.0.0",
    "postcss": "^8.0.0",
    "eslint": "^8.0.0",
    "eslint-config-next": "^13.0.0"
  }
}

// tailwind.config.js
module.exports = {
  content: ["./pages/**/*.{js,ts,jsx,tsx}", "./components/**/*.{js,ts,jsx,tsx}"],
  theme: { extend: {} },
  plugins: []
};

// postcss.config.js
module.exports = { plugins: { tailwindcss: {}, autoprefixer: {} } };

// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = { reactStrictMode: true };
module.exports = nextConfig;

// styles/globals.css
@tailwind base;
@tailwind components;
@tailwind utilities;

// components/Header.tsx
import Link from 'next/link';
import { useState } from 'react';
import { motion, AnimatePresence } from 'framer-motion';

const services = [
  { name: 'Corporate Catering', href: '/corporate-catering' },
  { name: 'Event Planning', href: '/event-planning' },
  { name: 'Social Celebrations', href: '/social-celebrations' },
  { name: 'Specialty Menus', href: '/specialty-menus' },
  { name: 'Beverage Services', href: '/beverage-services' },
  { name: 'On-Site Coordination', href: '/on-site-coordination' },
  { name: 'Post-Event Wrap-Up', href: '/post-event-wrapup' }
];

export default function Header() {
  const [open, setOpen] = useState(false);
  return (
    <header className="fixed w-full z-50 backdrop-blur bg-white/70 shadow">
      <div className="container mx-auto flex items-center justify-between p-4">
        <Link href="/"><a className="text-2xl font-bold">TCEPS</a></Link>
        <nav className="flex items-center space-x-6">
          <div className="relative">
            <button
              onMouseEnter={() => setOpen(true)}
              onMouseLeave={() => setOpen(false)}
              className="font-medium"
            >
              Services
            </button>
            <AnimatePresence>
              {open && (
                <motion.div
                  onMouseEnter={() => setOpen(true)}
                  onMouseLeave={() => setOpen(false)}
                  initial={{ opacity: 0, y: -10 }}
                  animate={{ opacity: 1, y: 0 }}
                  exit={{ opacity: 0, y: -10 }}
                  className="absolute left-0 mt-2 w-64 bg-white shadow-lg rounded-lg z-50"
                >
                  <div className="flex flex-col p-4">
                    {services.map(s => (
                      <Link key={s.href} href={s.href}>
                        <a className="px-2 py-1 rounded hover:bg-gray-100 transition">{s.name}</a>
                      </Link>
                    ))}
                  </div>
                </motion.div>
              )}
            </AnimatePresence>
          </div>
          <Link href="/#contact">
            <a className="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition">Contact</a>
          </Link>
        </nav>
      </div>
    </header>
  );
}

// components/ServiceCard.tsx
import { motion } from 'framer-motion';
export interface ServiceCardProps { title: string; description: string; href: string; }
export default function ServiceCard({ title, description, href }: ServiceCardProps) {
  return (
    <motion.div
      className="p-6 bg-white rounded-lg shadow hover:shadow-md transition"
      whileHover={{ scale: 1.02 }}
    >
      <h3 className="text-xl font-semibold mb-2">{title}</h3>
      <p className="text-gray-600 mb-4">{description}</p>
      <a href={href} className="text-blue-600 hover:underline">Learn More →</a>
    </motion.div>
  );
}

// components/ContactForm.tsx
import { useState } from 'react';
export default function ContactForm() {
  const [status, setStatus] = useState<'idle'|'sending'|'success'|'error'>('idle');
  const [form, setForm] = useState({ name: '', email: '', message: '' });
  const handleChange = (e: React.ChangeEvent<HTMLInputElement|HTMLTextAreaElement>) =>
    setForm({ ...form, [e.target.name]: e.target.value });

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setStatus('sending');
    try {
      const res = await fetch('/api/contact', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(form)
      });
      if (res.ok) setStatus('success'); else throw new Error();
    } catch {
      setStatus('error');
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      className="max-w-lg mx-auto p-6 bg-white rounded-lg shadow"
    >
      <h2 className="text-2xl font-bold mb-4 text-center">Contact Us</h2>
      {status === 'success' && <p className="text-green-600 mb-4 text-center">Thank you! We'll be in touch.</p>}
      {status === 'error' && <p className="text-red-600 mb-4 text-center">Something went wrong. Try again.</p>}
      <input
        name="name"
        placeholder="Name"
        required
        value={form.name}
        onChange={handleChange}
        className="w-full mb-3 p-2 border rounded"
      />
      <input
        type="email"
        name="email"
        placeholder="Email"
        required
        value={form.email}
        onChange={handleChange}
        className="w-full mb-3 p-2 border rounded"
      />
      <textarea
        name="message"
        placeholder="Message"
        required
        value={form.message}
        onChange={handleChange}
        className="w-full mb-3 p-2 border rounded h-32"
      ></textarea>
      <button
        type="submit"
        disabled={status === 'sending'}
        className="w-full py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition"
      >
        {status === 'sending' ? 'Sending...' : 'Send Message'}
      </button>
    </form>
  );
}

// components/Footer.tsx
export default function Footer() {
  return (
    <footer className="bg-gray-800 text-white py-6 mt-16">
      <div className="container mx-auto text-center">
        <p>&copy; {new Date().getFullYear()} TCEPS. All rights reserved.</p>
      </div>
    </footer>
  );
}

// pages/_app.tsx
import '../styles/globals.css';
import type { AppProps } from 'next/app';
export default function App({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />;
}

// pages/index.tsx
import Header from '../components/Header';
import ServiceCard from '../components/ServiceCard';
import ContactForm from '../components/ContactForm';
import Footer from '../components/Footer';
const services = [
  { title: 'Corporate Catering', description: 'Buffets, boxed meals, coffee breaks & gala dinners.', href: '/corporate-catering' },
  { title: 'Event Planning', description: 'Venue scouting, themed décor & full logistics.', href: '/event-planning' },
  { title: 'Social Celebrations', description: 'Weddings, anniversaries & cultural ceremonies.', href: '/social-celebrations' },
  { title: 'Specialty Menus', description: 'Live stations, dietary menus & artisan bars.', href: '/specialty-menus' },
  { title: 'Beverage Services', description: 'Open bars, signature cocktails & wine pairings.', href: '/beverage-services' },
  { title: 'On-Site Coordination', description: 'Day-of management & vendor liaison.', href: '/on-site-coordination' },
  { title: 'Post-Event Wrap-Up', description: 'Cleanup, surveys & final reporting.', href: '/post-event-wrapup' }
];
export default function Home() {
  return (
    <>
      <Header />
      <motion.main initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 0.5 }} className="pt-24 container mx-auto px-4">
        <section className="text-center py-16">
          <h1 className="text-4xl font-bold mb-4">TOTAG Catering & Event Planning</h1>
          <p className="text-gray-700 max-w-2xl mx-auto">
            Creating unforgettable moments with culinary excellence and meticulous event management.
          </p>
        </section>
        <section className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-16">
          {services.map(s => <ServiceCard key={s.title} {...s} />)}
        </section>
      </motion.main>
      <section id="contact" className="py-16 bg-gray-50"><ContactForm /></section>
      <Footer />
    </>
  );
}

// pages/corporate-catering.tsx
import Header from '../components/Header';
import Footer from '../components/Footer';
import ContactForm from '../components/ContactForm';
import { motion } from 'framer-motion';
export default function CorporateCatering() {
  return (
    <>
      <Header />
      <motion.main initial={{ y: 20, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ duration: 0.4 }} className="pt-24 container mx-auto px-4">
        <h1 className="text-3xl font-bold mb-4">Corporate Catering</h1>
        <p className="text-gray-700 mb-6">
          Elevate your corporate events with our bespoke catering solutions. From continental breakfasts and working lunches to gala dinners and executive retreats, we tailor each menu to your brand and dietary needs.
        </p>
        <ul className="list-disc list-inside mb-6 text-gray-600">
          <li>Custom menu planning aligned with corporate objectives</li>
          <li>Dietary accommodations: vegan, gluten-free, halal</li>
          <li>Branded presentation and service staff in your company’s colors</li>
        </ul>
      </motion.main>
      <section className="py-16 bg-gray-50"><ContactForm /></section>
      <Footer />
    </>
  );
}

// pages/event-planning.tsx
import Header from '../components/Header';
import Footer from '../components/Footer';
import ContactForm from '../components/ContactForm';
import { motion } from 'framer-motion';
export default function EventPlanning() {
  return (
    <>
      <Header />
      <motion.main initial={{ y: 20, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ duration: 0.4 }} className="pt-24 container mx-auto px-4">
        <h1 className="text-3xl font-bold mb-4">Event Planning & Design</h1>
        <p className="text-gray-700 mb-6">
          From concept to completion, our event planners handle logistics, décor, and AV coordination—ensuring a seamless experience. We manage venue selection, staging, lighting, and all on-site details.
        </p>
        <ul className="list-disc list-inside mb-6 text-gray-600">
          <li>Venue scouting and contract negotiation</li>
          <li>Themed décor, staging, and custom branding</li>
          <li>Audio-visual setup and tech support</li>
        </ul>
      </motion.main>
      <section className="py-16 bg-gray-50"><ContactForm /></section>
      <Footer />
    </>
  );
}

// pages/social-celebrations.tsx
import Header from '../components/Header';
import Footer from '../components/Footer';
import ContactForm from '../components/ContactForm';
import { motion } from 'framer-motion';
export default function SocialCelebrations() {
  return (
    <>
      <Header />
      <motion.main initial={{ y: 20, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ duration: 0.4 }} className="pt-24 container mx-auto px-4">
        <h1 className="text-3xl font-bold mb-4">Social Celebrations</h1>
        <p className="text-gray-700 mb-6">
          Celebrate life’s milestones with flair. Whether weddings, anniversaries, or birthday galas, we craft personalized experiences with custom menus and décor that reflect your vision.
        </p>
        <ul className="list-disc	list-inside mb-6 text-gray-600">
          <li>Wedding receptions with curated multi-course meals</li>
          <li>Anniversary and birthday packages with thematic styling</li>
          <li>Cultural ceremonies with authentic menu options</li>
        </ul>
      </motion.main>
      <section className="py-16 bg-gray-50"><ContactForm /></section>
      <Footer />
    </>
  );
}

// pages/specialty-menus.tsx
import Header from '../components/Header';
import Footer from '../components/Footer';
import ContactForm from '../components/ContactForm';
import { motion } from 'framer-motion';
export default function SpecialtyMenus() {
  return (
    <>
      <Header />
      <motion.main initial={{ y: 20, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ duration: 0.4 }} className="pt-24 container mx-auto px-4">
        <h1 className="text-3xl font-bold mb-4">Specialty Menus</h1>
        <p className="text-gray-700 mb-6">
          Delight your guests with live cooking stations, carving stations, sushi bars, and more. Our specialty menus cater to dietary restrictions and gourmet palates alike.
        </p>
        <ul className="list-disc	list-inside mb-6 text-gray-600">
          <li>Live cooking and carving stations</li>
          <li>Sushi and artisan cocktail bars</li>
          <li>Gluten-free, vegan, and kosher options</li>
        </ul>
      </motion.main>
      <section className="py-16 bg-gray-50"><ContactForm /></section>
      <Footer />
    </>
  );
}

// pages/beverage-services.tsx
import Header from '../components/Header';
import Footer from '../components/Footer';
import ContactForm from '../components/ContactForm';
import { motion } from 'framer-motion';
export default function BeverageServices() {
  return (
    <>
      <Header />
      <motion.main initial={{ y: 20, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ duration: 0.4 }} className="pt-24 container mx-auto px-4">
        <h1 className="text-3xl	font-bold mb-4">Beverage Services</h1>
        <p className="text-gray-700 mb-6">
          From open bars and signature cocktails to curated wine pairings and tea & coffee stations, our bar services elevate any event with professional flair.
        </p>
        <ul className="list-disc	list-inside mb-6 text-gray-600">
          <li>Open bar setups with professional bartenders</li>
          <li>Custom cocktail menu development</li>
          <li>Wine and spirit pairings by sommeliers</li>
        </ul>
      </motion.main>
      <section className="py-16	bg-gray-50"><ContactForm /></section>
      <Footer />
    </>
  );
}

// pages/on-site-coordination.tsx
import Header from '../components/Header';
import Footer from '../components/Footer';
import ContactForm from '../components/ContactForm';
import { motion } from 'framer-motion';
export default function OnSiteCoordination() {
  return (
    <>
      <Header />
      <motion.main	initial={{ y: 20, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ duration: 0.4 }} className="pt-24 container mx-auto px-4">
        <h1 className="text-3xl font-bold mb-4">On-Site Coordination</h1>
        <p className="text-gray-700 mb-6">
          Our experienced coordinators manage every detail on the day of your event, liaising with vendors, overseeing timelines, and ensuring seamless execution so you can enjoy stress-free hospitality.
        </p>
        <ul className="list-disc list-inside mb-6 text-gray-600">
          <li>Vendor liaison and logistics management</li>
          <li>Timeline tracking and real-time adjustments</li>
          <li>Guest liaison and service supervision</li>
        </ul>
      </motion.main>
      <section className="py-16	bg-gray-50"><ContactForm /></section>
      <Footer />
    /></>
  );
}

// pages/post-event-wrapup.tsx
import Header from '../components/Header';
import Footer from '../components/Footer';
import ContactForm from '../components/ContactForm';
import { motion } from 'framer-motion';
export default function PostEventWrapUp() {
  return (
    <>
      <Header />
      <motion.main initial={{ y: 20, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ duration: 0.4 }} className="pt-24 container mx-auto px-4">
        <h1 className="text-3xl font-bold mb-4">Post-Event Wrap-Up</h1>
        <p className="text-gray-700 mb-6">
          We finalize every event with thorough cleanup, guest feedback collection, and detailed reporting. Our post-event wrap-up ensures all loose ends are tied and insights are captured for future success.
        </p>
        <ul className="list-disc list-inside mb-6 text-gray-600">
          <li>On-site cleanup and teardown services</li>
          <li>Guest experience surveys and analysis</li>
          <li>Comprehensive event report and debrief</li>
        </ul>
      </motion.main>
      <section className="py-16 bg-gray-50"><ContactForm /></section>
      <Footer />
    </>
  );
}

// pages/api/contact.ts
import type { NextApiRequest, NextApiResponse } from 'next';
import nodemailer from 'nodemailer';
export default async function handler(req: NextApiRequest, res: NextApiResponse) {
  if (req.method !== 'POST') return res.status(405).end();
  const { name, email, message } = req.body;
  if (!name || !email || !message) return res.status(400).json({ error: 'All fields are required' });
  const transporter = nodemailer.createTransport({
    host: process.env.SMTP_HOST,
    port: 587,
    auth: { user: process.env.SMTP_USER, pass: process.env.SMTP_PASS }
  });
  try {
    await transporter.sendMail({
      from: `"TCEPS" <${process.env.SMTP_USER}>`,
      to: 'catering@totag-group.com',
      subject: `Contact from ${name}`,
      text: message
    });
    return res.status(200).json({ success: true });
  } catch {
    return res.status(500).json({ error: 'Mail send failed' });
  }
}
